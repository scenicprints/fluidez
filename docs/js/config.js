// Firebase project settings.
//
// These are public by design — a web app config is not a secret; access is
// controlled by the Firestore security rules, not by hiding this file.
// Leaving projectId empty makes the app run entirely offline-and-local, which
// is a valid mode: everything works, nothing syncs.

export const firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
