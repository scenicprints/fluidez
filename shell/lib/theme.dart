import 'package:flutter/material.dart';

// Volcanic night — the same palette as the web app, so the only native chrome
// (the update banner) blends into the page instead of sitting on top of it.

const Color kBg = Color(0xFF14100E);     // basalt
const Color kCard = Color(0xFF201A18);   // raised surface
const Color kBorder = Color(0xFF3A302C);
const Color kInk = Color(0xFFEFE7DC);
const Color kMuted = Color(0xFFA99C8E);
const Color kAccent = Color(0xFFE8A33D); // gold — the fritanga bulb
const Color kJade = Color(0xFF34B396);
const Color kWarn = Color(0xFFD9613F);   // clay
